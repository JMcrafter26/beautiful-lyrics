# Fonts

This directory should contain the custom BeautifulLyrics font files (woff2 format):

- `LyricsRegular.woff2` (weight 400)
- `LyricsMedium.woff2` (weight 500)
- `LyricsSemibold.woff2` (weight 600)
- `LyricsBold.woff2` (weight 700)

These fonts are referenced in `Extension/Source/main.ts`.
