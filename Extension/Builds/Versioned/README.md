# Versioned Builds

This directory contains versioned built extension files (e.g. `beautiful-lyrics@5.1.0.mjs` and `beautiful-lyrics@5.1.0.css`).

These are generated by running the release build script (`deno task release` inside `Extension/`).
